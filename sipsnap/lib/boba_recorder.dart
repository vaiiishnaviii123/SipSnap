// boba_recorder.dart
import 'package:flutter/material.dart';

class BobaRecorder extends StatelessWidget {
  const BobaRecorder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Boba Photos'),
      ),
    );
  }
}
